package com.jorgeadammo.sistemas.entity;


import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.Table;


//@Entity
//@Table(schema="CNCRS", name="STA_STATUS_CONCURSO")
public class Status extends PersistentEntity{
	/*
	private static final long serialVersionUID = 1L;
	
	@Column(length=100, nullable=false, unique=true, name="STA_STATUS")
	private String concursoStatus;
	
	@Column(length=255, name="STA_DESC")
	private String descricao;
	
	@Column(name="STA_ATIVO")
	private boolean ativo;
	
	//
	
	public Status() {
	}

	public Status(Long id, String concursoStatus, String descricao, boolean ativo, 
			Date inclusion, Date lastEdition, String includedBy, String  editedBy) {
		this.id = id;
		this.concursoStatus = concursoStatus;
		this.descricao = descricao;
		this.ativo = ativo;
		this.inclusion = inclusion;
		this.lastEdition = lastEdition;
		this.includedBy = includedBy;
		this.editedBy = editedBy;
	}
	
	@Override
	public String toString() {
		return "Status [id=" + id + ", concursoStatus=" + concursoStatus + ", descricao=" + descricao
				+ ", ativo=" + ativo + ", inclusion=" + inclusion + ", lastEdition=" + lastEdition
				+ ", includedBy=" + includedBy + ", editedBy=" + editedBy + "]";
	}
	*/
}
